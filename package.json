{
  "name": "torque-handler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev:fs": "concurrently -n api,ui -c blue,green \"node index\" \"npm --prefix frontend run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.32.0",
    "body-parser": "^1.19.0",
    "dotenv": "^17.4.2",
    "express": "^4.17.1",
    "http": "0.0.0",
    "mongoose": "^7.8.0",
    "ws": "^7.1.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
